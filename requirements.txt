# EEG Visualizer — runtime dependencies

# GUI
PyQt6>=6.6.0

# Plotting
pyqtgraph>=0.13.3
matplotlib>=3.7.0

# EEG I/O and processing
mne>=1.6.0
numpy>=1.24.0
scipy>=1.11.0

# Optional: only needed to *write* EDF files (e.g. to build test fixtures).
# Reading does not require it.
# edfio>=0.4.0
